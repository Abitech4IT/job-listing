import BackgroundImage from "../components/BackgroundImage";
import PageLayout from "../components/PageLayout";

const Layout = () => {
  return <PageLayout header={<BackgroundImage />} />;
};

export default Layout;
